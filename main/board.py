import random
import property

class Board:
    
    def __init__(self):
        self.board = generateBoard()
        self.diceOne = 0
        self.diceTwo = 0
        
    def __generateBoard__():
        board = []
        # TODO Think about generating the board later
        return board

    def rollDices():
        self.diceOne = random.randint(1, 6)
        self.diceTwo = random.randint(1, 6)
